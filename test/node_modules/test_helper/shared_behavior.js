//-- load shared behaviors
var shared = require("require-all")(TEST_SHARED_BEHAVIORS_PATH);

global.itShouldBehaveLike = function(key) {
  var args = Array.prototype.slice.call(arguments, 1);
  var func = shared[key];
  if (typeof func == "function") {
    func.apply(this, args);
  }
};

